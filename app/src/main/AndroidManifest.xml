<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestTrack">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".paciente.CalendarioPacienteActivity" />
        <activity android:name=".paciente.PrincipalPacienteActivity" />
        <activity android:name=".paciente.ChatPacienteActivity" />
        <activity android:name=".paciente.MeuPerfilPacienteActivity" />
        <activity android:name=".paciente.MinhasNotasPacienteActivity" />
        <activity android:name=".paciente.LoginPacienteActivity"/>
        <activity android:name=".paciente.CadastroPacienteActivity"/>

    </application>

</manifest>
